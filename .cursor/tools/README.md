# Tools

- Add helper scripts and MCP tool configs here when needed.
- For external references, see `https://github.com/Entropicsky/mycursorrules` (import selectively when network access is available).
- Keep tools documented with usage and non-interactive flags for CI.
